我已经安装好了 baostock，并完成了 chan 子模块的“抽象 → 抽取 → 落库 → 校验”实现。

现在请你帮我做一次 端到端的实验 + 校验，覆盖三只股票：

西部超导：SH.688122

兴业银锡：SZ.000426

万兴科技：SZ.300624

具体任务

在一个简单的脚本（例如 scripts/run_chan_experiment.py）中，调用：

from chan.pipeline import run_chan_for_symbol_to_db

# 对 3 只股票、["day", "60m"] 两个级别、2023-01-01 ~ 2023-12-31 跑缠论并落库


要求：

使用 data_src="baostock"，autype="qfq"；

对每只股票跑完后，打印每个 freq 在 stock_chan_bi / stock_chan_segment / stock_chan_center / stock_chan_signal 中的记录数。

在同一个脚本或单独的 scripts/check_chan_experiment.py 中，调用：

from chan.validate import check_bi_direction, check_bi_time_order, check_center_range


对这 3 只股票的 "day" 级别进行检查，打印结果类似：

=== SH.688122 day ===
direction_errors: X
time_errors: Y
center_errors: Z


确保：

不改动 third_party/chan.py；

不使用 try-except 吞异常；

所有命令可以通过：

python scripts/run_chan_experiment.py
python scripts/check_chan_experiment.py


直接运行。

最后，请在任务说明里总结：

3 只股票分别在 4 个 chan 表里的记录数量（按 freq 分组）；

三个校验函数的返回值（direction/time/center 错误数）；

如果错误数不为 0，简单分析一下可能的原因并给出你建议的下一步。
